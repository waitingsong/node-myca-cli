
export const cmdSet = new Set(['init', 'initca', 'issue', 'initcenter'])

export const tw1 = '\t'.repeat(1)
export const tw2 = '\t'.repeat(2)
export const tw3 = '\t'.repeat(3)

export const initialCliArgs = {
  cmd: void 0,
  options: null,
  needHelp: false,
  debug: false,
}
